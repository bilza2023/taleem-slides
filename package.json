{
  "name": "taleem-slides",
  "version": "0.4.0",
  "type": "module",
  "description": "Convert json taleem schema into html for slides",

  "exports": {
    ".": "./src/index.js"
  },

  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ui": "vitest --ui",
    "lint": "echo \"no lint yet\""
  },

  "devDependencies": {
    "vite": "^5.0.0",
    "vitest": "^1.5.0"
  }
}
